# Context Bootstrap

When a task requires understanding project architecture, data flow, cross-module relationships, or the ETL pipeline — read `graphify-out/graph.json` first. It contains a pre-computed knowledge graph of the entire codebase: entities, relationships, rationale decisions, and dashboard metrics.

Skip the graph for narrowly scoped tasks (typo fixes, single-file edits where you already know the file).
